# v1.5.1 local release evidence

## Scope and environment

- Date: 2026-09-01, Asia/Shanghai.
- Hardware: Apple M3, 24 GiB memory, native `arm64`.
- System: macOS 26.5.2 (25F84).
- Toolchain: Xcode 26.6 (17F113), Release configuration, macOS 13.0 deployment target.
- Bundle: MacWubi 1.5.1 (13), `org.macwubi.inputmethod.MacWubi`.
- Signing: Apple Development only. Developer ID Application, notarization, staple and public Gatekeeper
  distribution gates were not run and are not claimed.

## Automated gates

`Scripts/test.sh` passed after the release metadata contract was updated for 1.5.1 (13):

- MacWubiTests: 270/270 passed.
- MacWubiReleaseTests: 10/10 passed.
- Deterministic lexicon fixture compilation and known-vector verification passed.

The Apple Development Release build completed with:

```text
MACWUBI_CODE_SIGN_IDENTITY="<configured Apple Development identity>" Scripts/build-release.sh
```

The following gates passed against the exact Release application:

- `Scripts/verify-release.sh`: thin arm64, Hardened Runtime, strict signature, empty entitlements,
  system-only dependencies, non-sandboxed InputMethodKit metadata and bundled license/provenance.
- `Scripts/privacy-audit.sh`: no network capability, prohibited network/global-input symbols or unsafe
  diagnostic surface.
- Bundle version readback: 1.5.1 (13).
- Main executable SHA-256:
  `4f19a411828aea466d9be4d78ba39f0a10e14c763283cd0e6173c1c8c02bc777`.

## Package, installation and functional verification

- Local Apple Development archive:
  `.build/releases/MacWubi-v1.5.1-apple-development.zip`.
- Archive SHA-256:
  `3e3b265afd32be18f67211253904963d183a54d29bee2148a762ee6b28a85ef7`.
- The archive was extracted to a fresh temporary directory and passed `Scripts/verify-release.sh` again.
- `Scripts/install.sh` atomically replaced `/Library/Input Methods/MacWubi.app`; installed version and
  executable hash matched the verified build, and the system loaded that installed executable.
- TextEdit with the installed input source committed `你好` for `wqvb` + Space.
- TextEdit with the installed input source committed `的` for `z` + Space, proving that lowercase `z`
  entered the mixed-Pinyin candidate path instead of passing through as `z `.

The development-signed archive remains local and is not a public install artifact. The public v1.5.1
release is limited to the immutable Git tag and GitHub-generated source archives.
