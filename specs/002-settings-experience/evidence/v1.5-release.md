# v1.5 Release Evidence

**Date**: 2026-09-01

**Version**: 1.5 (12)

**Task**: T105 local signed package and installation validation

## Environment

- Hardware: MacBook Air (Mac15,12), Apple M3, 24 GB memory.
- Architecture: arm64.
- macOS: 26.5.2 (25F84).
- Xcode: 26.6 (17F113), Release configuration for the release candidate.
- Dictionary fixtures: bundled `wb86.bin`, `pinyin-simp.bin`, script conversion image, and the
  deterministic acceptance fixtures exercised by `Scripts/test.sh`.

## Automated validation

```bash
Scripts/test.sh
```

- Main XCTest suite: 268 passed, 0 failed.
- Release XCTest suite: 10 passed, 0 failed.
- Deterministic dictionary compiler and lexicon fixture verification: passed.
- The strict `< 2 ms` lookup performance contract remained green in the full suite.

## Release candidate

```bash
MACWUBI_CODE_SIGN_IDENTITY="Apple Development: luoagol@gmail.com (6XRC4PBH7N)" \
  Scripts/build-release.sh
Scripts/verify-release.sh .build/xcode/Build/Products/Release/MacWubi.app
Scripts/privacy-audit.sh .build/xcode/Build/Products/Release/MacWubi.app
ditto -c -k --sequesterRsrc --keepParent \
  .build/xcode/Build/Products/Release/MacWubi.app \
  .build/release/MacWubi-v1.5.zip
```

- Xcode Release build: passed with an existing Swift 6 actor-isolation warning; the project remains in
  Swift 5 language mode.
- Bundle version: 1.5 (12).
- Main executable architecture: exactly `arm64`.
- Hardened Runtime, strict signature, empty entitlement policy, system-only dependencies,
  InputMethodKit metadata and bundled license/provenance verification: passed.
- Privacy audit: zero network capability, redacted diagnostics and bounded local data passed.
- The ZIP was extracted to a new temporary directory and the extracted application passed the same
  release verification before installation.
- Archive SHA-256: `11892ee61b208d797b67271e65aba61adf37654da8198c8109d856361725174e`.
- Main executable SHA-256: `18d75675e2f94607666e251017db1f17a5ad82d9f89b6a5bcb417f15514675c9`.
- WB86 image SHA-256: `45e8132d0d0cdd9f2662a6821dd4b886cb6f7272f7e41000175abb8c6a099ab0`.
- Pinyin image SHA-256: `0841a66e41e3768739b8d6370aaee14670d1c464a6c262c698f83488d12f411b`.

## Local installation

```bash
Scripts/install.sh .build/xcode/Build/Products/Release/MacWubi.app
Scripts/verify-release.sh '/Library/Input Methods/MacWubi.app'
```

- The installer staged and verified the candidate before atomically replacing the existing bundle.
- The installed bundle reports 1.5 (12), passed release verification, and started successfully.
- The installed executable SHA-256 exactly matches the verified build product.
- User settings, user lexicon and learning snapshots were not modified by the bundle replacement.
- Automated TextEdit key injection could not run because macOS denied `osascript` Accessibility
  permission. Physical `wqvb` + Space commit and the broader cross-application matrix remain manual.

## Distribution boundary

The locally installed candidate uses an Apple Development identity. Developer ID signing, notarization,
staple and Gatekeeper assessment are not claimed. The public v1.5 Release provides the immutable source
tag and GitHub-generated source archives only; the local Apple Development ZIP is retained as verification
evidence and is not published as a portable installation asset.
